get:
  tags: [HubPackages]
  summary: Get hub package detail
  operationId: getHubPackage
  parameters:
  - name: owner
    in: path
    required: true
    schema:
      type: string
  - name: name
    in: path
    required: true
    schema:
      type: string
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            $ref: '../components/schemas/index.yaml#/HubPackageDetail'
    '404':
      $ref: '../components/responses.yaml#/NotFound'
