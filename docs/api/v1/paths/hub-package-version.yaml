get:
  tags: [HubPackages]
  summary: Get hub package version detail
  operationId: getHubPackageVersion
  parameters:
  - name: packageName
    in: path
    required: true
    schema:
      type: string
  - name: version
    in: path
    required: true
    schema:
      type: string
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            $ref: '../components/schemas/index.yaml#/HubPackageVersionDetail'
    '404':
      $ref: '../components/responses.yaml#/NotFound'
