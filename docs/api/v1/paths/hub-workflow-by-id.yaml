get:
  tags: [HubWorkflows]
  summary: Get hub workflow detail
  operationId: getHubWorkflow
  parameters:
  - name: workflowId
    in: path
    required: true
    schema:
      type: string
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            $ref: '../components/schemas/index.yaml#/HubWorkflowDetail'
    '404':
      $ref: '../components/responses.yaml#/NotFound'
